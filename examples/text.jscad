function main() {
    util.init(CSG);


    // create a label, place it on top of the cube
    // and center it on the top face
    var label = util.text('H');

    return label;
}

// include:js
// endinject
